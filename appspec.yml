version: 0.0
os: linux
files:
    - source: /
      destination: /var/www/html
hooks:
BeforeInstall:
    - location: before.sh
      timeout: 300
ApplicationStart:
    - location: applicationstart.sh
      timeout: 3000

